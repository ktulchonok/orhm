<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : 'History Odessa' %></title>
  <meta name="description" content="<%= content_for?(:description) ? yield(:description) : 'History Odessa' %>">
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <%= yield(:head) %>
</head>
<body class="<%= controller_name %> <%= action_name %> desktop">
<header>
  <%= render 'layouts/header' %>
</header>
<%= render 'layouts/navigation' %>
<div class="modal fade bs-example-modal-sm" tabindex="-1" role="dialog" aria-labelledby="mySmallModalLabel">
  <div class="modal-dialog" role="document" style="width: 400px">
    <div class="modal-content" style="padding: 20px 40px">
      <section class="subscribe">
        <h4 style="font-family: Raleway, serif; text-align: center; font-size: 20px; text-transform: uppercase">
          Будьте в курсе наших событий
        </h4>
        <%= render 'clients/form' %>
        <style>
          .form-inputs input:focus{
            border-color: #3AA3A4;
          }
          .form-actions {
            display: flex;
            justify-content: center;
          }
          .form-actions input {
            text-transform: uppercase;
            background: #d1e1e6 !important;
          }
          .form-actions input:hover{
            /*box-shadow: 0 0 10px 0 rgba(0,0,0,.3);*/
            background-color: #3AA3A4 !important;
            color: white;
          }
        </style>
      </section>
    </div>
  </div>
</div>
<div id="content">
  <div id="wrapper">
    <%= render 'layouts/messages' %>
    <div class="col-lg-8 col-xs-8 zero-m-p">
      <%= yield %>
    </div>
    <aside class="col-lg-4 col-xs-4 zero-m-p">
      <%= render 'layouts/contacts' %>
      <%= render 'layouts/exposition' %>
    </aside>
  </div>
</div>
<footer>
  <%= render 'layouts/footer' %>
</footer>
</body>
</html>
